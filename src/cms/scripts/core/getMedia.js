import shoppableWrapper from 'infinite/cms/scripts/core/shoppableWrapper';
import generateBackground from 'infinite/cms/scripts/core/CMS-Background';

export default (data, is_bg_media, module_name, module_index, submodule_index) => { return null; };