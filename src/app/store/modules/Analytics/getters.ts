import { defineGetters } from 'direct-vuex';
import { AnalyticsGetterContext } from './';

export default defineGetters<Analytics.State>()({
  // myGetter(state): string {
    // return state.my_value ? `Yes` : `No`;
  // },
});